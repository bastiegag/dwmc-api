# Changesets

This directory stores release notes for backend version changes.

- Add a changeset for user-facing or operational changes that should be released.
- Use `npm run changeset` to create one.
- Use `npm run version` in the Version PR to update `package.json` and `CHANGELOG.md`.
- Use `npm run release` only for local tag creation after a versioned release is ready.
